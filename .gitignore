*.o
*.bin
*.elf
*.map
# Kernel modules that get patched into the kernel after the kernel is
# built. to add it into the kernel, the build/{arch}/kImage.bin must be ready
# then run make patch_mods
*.kmod